import 'package:flutter/material.dart';

class HomeBody extends StatelessWidget {
  final int currentIndex;

  const HomeBody({super.key, required this.currentIndex});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
